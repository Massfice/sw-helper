{if isset($all_vars) && is_array($all_vars)}

{foreach $all_vars as $var}
<div>{$var}</div>
<div>------------------------------------------------------------------------------------------------------------------------------------------------</div>
{/foreach}

{/if}
